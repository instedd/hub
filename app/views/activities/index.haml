.row-fluid
  .span12
    %h4 Connectors Activity
    - if Activity.enabled?
      - activities.each do |activity|
        .row-fluid
          = activity.fields
    - else
      %p Activities logs are not supported on this instance
